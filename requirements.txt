# Core training stack (install a CUDA build of torch that matches your driver first)
# e.g.  pip install torch --index-url https://download.pytorch.org/whl/cu121
# Gemma 4 needs recent unsloth and transformers releases.
unsloth>=2026.8.18
trl>=0.9.0
transformers>=5.5.0
datasets
accelerate
peft
bitsandbytes

# Reward: syntactic meter check
skrutable
indic-transliteration

# Logging
wandb
